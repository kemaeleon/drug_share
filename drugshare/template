""" A django model generated with django-csvimport csvinspect
    which used OKN messytables to guess data types - may need some manual tweaks!
"""
from django.db import models


class CsvimportNew(models.Model):
    ''' Autogenerated model file csvimportnew Sun Mar 22 09:16:37 2020 '''

    crystal_id = models.CharField(max_length=10, primary_key=True, blank=False, null=False, default='')
    datasetcluster = models.IntegerField(blank=True, null=True, default=0)
    library_name = models.(blank=True, null=True)
    compound_smiles = models.(blank=True, null=True)
    modified_compound_smiles = models.(blank=True, null=True)
    compoundcode = models.(blank=True, null=True)
    status = models.(blank=True, null=True)
    site = models.(blank=True, null=True)
    initial_occupancy_estimate = models.DecimalField(max_digits=4, decimal_places=2, blank=True, null=True, default=0)
    peak_height_z_value = models.DecimalField(max_digits=4, decimal_places=2, blank=True, null=True, default=0)
    map_resol = models.DecimalField(max_digits=4, decimal_places=2, blank=True, null=True, default=0)
    confidence_annotation = models.CharField(max_length=5, blank=True, default='')

    class Meta:
        managed = False
        db_table = u'"csvimport_new"'
